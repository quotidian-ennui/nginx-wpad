name: docker-build-latest

on:
  push:
    branches: main
    paths-ignore:
      - "README.md"

jobs:
  build:
    if: ${{ github.actor != 'dependabot[bot]' }}
    uses: ./.github/workflows/docker-build-image.yml
    with:
      registry_push: true